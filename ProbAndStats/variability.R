x <- c(1.75, 2.66, 1.97, 2.17, 1.82)

xsum <- sum(x)
xm <- mean(x)
xmed <- median(x)
xsorted <- sort(x) # Sorts the list. Useful for finding modes.
xsd <- sd(x)
xrange <- range(x)
xvar <- var(x)

print(2.66 - 1.75)